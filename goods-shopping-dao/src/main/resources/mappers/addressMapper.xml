<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orders.AddressDao">

    <select id="getAllAddress" resultType="com.order.Address">
        select address_id,address_name,address_number,address_home from addres order by address_id
    </select>
</mapper>